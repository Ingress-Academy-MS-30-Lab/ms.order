server:
  port: ${PORT:8080}

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

spring:
  cloud:
    openfeign:
      caching:
        enabled: false

  liquibase:
    enabled: true
    change-log: classpath:/changelog/changelog-master.yml
    default-schema: public